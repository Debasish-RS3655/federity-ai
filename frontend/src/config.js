// defining a config file for all dynamic details
// Debashish Buragohain

const config = {
    defaultPeer: 'http://localhost:1234/gun',
    allowedEmailDomains: ['@gmail.com', 'nits.ac.in'],
    nodeByteSize: 512 // byte size that a single node holds
}
export default config;